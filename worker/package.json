{
  "name": "fpl-worker",
  "private": true,
  "type": "module",
  "description": "Cloudflare Worker that builds the FPL dashboard payload. No dependencies; deploy by pasting fpl-worker.js into the Cloudflare dashboard editor."
}
